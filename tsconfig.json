{
  "extends": "./tsconfig.base.json",
  "compilerOptions": { "lib": ["ESNext"], "types": ["bun"] },
  "include": [
    "src/**/*.ts",
    "scripts/**/*.ts",
    "tests/**/*.ts"
  ],
  "exclude": ["src/web", "node_modules", "dist", ".cache"]
}
